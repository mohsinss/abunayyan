// Empty stub so vite can resolve `import "server-only";` in test mode.
// Next.js provides the real guard at build time — in unit tests we simulate
// it being absent (effectively a no-op).
export {};
